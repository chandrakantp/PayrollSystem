@using edueTree.Helpers
@model IEnumerable<edueTree.Models.Structurepermission>

@{
    ViewBag.Title = "Index";
    Layout = null;
}

@*<section class="content-header">
        <h1>
            Manage Permission
            <small></small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Permission</a></li>
            <li class="active">Index</li>
        </ol>
    </section>*@


<div class="box box-success">
    <div class="box-header">
        <i class="fa fa-calendar"></i>
        <h3 class="box-title">Structure Permission List</h3>
    </div>
    <div class="box-body chat" id="chat-box">
        <div class="item">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayName("Structure Name")
                        </th>
                        <th>
                            @Html.DisplayName("Employee Name")
                        </th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var structure in Model.GroupBy(a => a.SalaryStructure.StuctureName))
                    {
                        <tr>
                            <td> @Html.DisplayFor(modelItem => structure.Key)</td>
                            <td></td>
                            <td></td>
                            <td></td>
                        </tr>
                        foreach (var item in structure)
                        {
                            <tr>
                                <td></td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Staff.staffCode)
                                    @Html.DisplayFor(modelItem => item.Staff.firstName)

                                    @Html.DisplayFor(modelItem => item.Staff.middleName)
                                    @Html.DisplayFor(modelItem => item.Staff.lastName)

                                </td>
                                <td>
                                    @Html.EncodedActionLink("<i class='fa fa-pencil-square'></i>", "Edit", "Structureassignment", new { id = item.Id }, null)
                                </td>
                                <td>
                                    <a data-toggle="modal" data-target="#myModalPermissionDelete" href='@Url.Action("Delete", new { id = item.Id,stid=item.StaffId,structureid=item.StructId })'><i class="fa fa-trash"></i></a>

                                </td>


                            </tr>
                        }

                    }
                </tbody>
            </table>
        </div>
    </div>
    <div class="modal fade" id="myModalPermissionDelete" role="dialog"></div>
    @*<div class="box-footer">
            <div class="input-group box-tools pull-right">
                <a href='@Url.Action("Create")' class="btn btn-success"><i class="fa fa-plus"></i></a>
            </div>
        </div>*@
</div>

<script>
    $(function () {
        //$.noConflict();
        $(".table").DataTable({
            "bSort": false,
            "searching": false,
            dom: 'Bfrtip',
            buttons: [
                "copy", "csv", "excel", "pdf", "print"
            ]
        });
        //$(document).ready(function () {
        //    $('.table').DataTable({

        //        dom: 'Bfrtip',
        //        buttons: [
        //            "copy", "csv", "excel", "pdf", "print"
        //        ]
        //    });
        //});

    });

</script>